{
  "name": "esbuild_test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.19.11",
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1"
  },
  "scripts": {
    "build:iife": "esbuild application.js --bundle --outfile=dist/out-iife.js --format=iife",
    "build:cjs": "esbuild application.js --bundle --outfile=dist/out-cjs.js --format=cjs",
    "build:esm": "esbuild application.js --bundle --outfile=dist/out-esm.js --format=esm"
  }
}
